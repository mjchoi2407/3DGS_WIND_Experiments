# 저해상도 세 메시의 천 굽힘 비교 준비

## 현재 상태

- 사용자 승인: 별도10초 `visual_v1`에서 기하 flag만 중단 조건에서 제외하고 원래 수치·flag·경고 횟수를 기록한다. 나머지 검산과 strict 모드는 유지한다. 시각 확인 전용·학습 부적격, 9개 씬 준비만 완료. GPU 마스크7조건 검증 통과. 기존 결과 변경 없음.

- 사용자 요청의 별도10초 비교 준비: `run_cloth_coarse_10s.sh`, 3메시×3굽힘 조건 각각600프레임. 기존4초 실행 보존, 프레임별 계측·2초 저장·Ctrl+C 정책 유지. 원본600프레임 바람·9개 plan 및 ready0 검증 완료, 본 계산 미실행.

- 사용자 정정: 기본 `gpu_v6`는 매1프레임 GPU 완료 확인·실패 상태 조회 후 시간 출력. 물리 상태 저장은2초 유지, 프레임 로그는 즉시 기록. 동기화 비용이 추가되므로 두 컴퓨터 모두 같은 버전끼리 비교한다. v5 이하 동결본은 보존한다.

- 터미널 요약 요청 반영: 현행 기본 `gpu_v5`. 확정 보고서의 새 구간만 프레임 범위·계산/검산·저장·전체 시간으로 표시한다. 기본2초 저장 경계를 유지하며 추가 GPU 동기화 없음. 실제 저장 보고서2구간 출력/중복 방지와 Ctrl+C 회귀3개 통과. 기존 실행/결과는 보존했다.

- Ctrl+C 요청 반영: 기본 `gpu_v4`에서 controller가 소유 worker 종료·2초 timeout 후 kill·다음 씬 중단을 처리한다. 미저장 구간 폐기, 확정 결과 보존, `interrupted` 자동 재개 금지. 중단 검사3개 통과. 조회 시 메인컴에는 천 실행이 없었고 cuDSS probe는 보존했다. 기존 동결 실행은 수정하지 않았다.

- 구간 시간 요청 반영: 현행 기본 출력은 `20260913_cloth_coarse_4s_gpu_v3`. 기존v2는 보존한다. 저장 구간별 계산·검산/경계 확인/전송·저장·해시 시간과 정상 처리 수, 장치·재개 초기화 조건을 기록한다. 기존 저장 경계 외 GPU 동기화는 추가하지 않았다. 짧은1프레임·재개1프레임의 구간 시간/합계/기록 보존 검증 통과. 전체4초 성능 측정은 사용자 실행 대기다.

- 확인 기준일: 2026-09-13. 현행 기본을 최적화 GPU 생성·독립 검산으로 변경했다. `20260913_cloth_coarse_4s_gpu_v2`의9개 씬 ready·0/240프레임을 확인했다. 구실행은 보존한다.
- 기본2초 무압축 저장·메시별 변경·정상 저장 경계 재개를 제공한다. 실패 프레임은 정상 데이터와 분리해 후보 상태·검사 상한을 남긴다.
- 별도 smoke 출력의9조건 첫1프레임·직사각형 재개1프레임, 검산 회귀8개·CPU18개 통과. [원본 hash·결과](../R1_teacher_velocity_reset/timestep_search/cloth_coarse/gpu_validation.json).
- 사용자가 전달한 기하 실패5개·완료4개는 [보고서 해석](../R1_teacher_velocity_reset/timestep_search/cloth_coarse/README.md#사용자-보고-기하-실패의-해석)에 분리했다. 서브컴 공유 run에서 원본을 확인했다. manifest393개 해시·새 GPU 비교의 입력 동일성 확인, 실패5건은 기하 flag만 발생했다. 실제 교차 판정은 미실행이다.
- 큰 회전·접힘에서 현행 충분조건이 실패할 수 있다. GPU 전환이 기하 오류의 해결은 아니며 인증 Gate·물성은 변경하지 않았다.
- 직사각형n16·수정된 삼각 깃발n12·손수건12×8에서 기존 굽힘·1/10·1/100을 각각4초 비교한다.
- 늘어남 저항·질량·고정 구간·바람·시간 간격·수치 허용오차를 유지한다. 손수건의 실제 집게 폭도 보존했다.
- [원본·설정·hash·검증·실행 및 재생 명령](../R1_teacher_velocity_reset/timestep_search/cloth_coarse/README.md).
- 백그라운드 실행·파일 로그·읽기 전용 상태 조회·확정 프레임 재개를 제공한다. 기존 실행은 별도로 보존했다.
- 이전 준비의 CPU17개 구현 검사와 실제9개 모델의 물성·기하·동결 검증 완료. 재준비·상태 조회 후 설정과 보고서 hash 동일.
- 전체4초 수렴·천다운 움직임·고해상도/10초 재검증은 미완료다. 자동 물성 채택·학습 발행·Gate 해제 없음.
- [코드 연결 기록](../../code/sessions/2026-09-13_02_cloth_coarse_preparation.md). code/experiments 변경만 있으며 commit·push 없음.
- 다음: 사용자가 실행한 결과에서 수치 통과와 몸체 굽힘 개선을 구분해 판정한다.
