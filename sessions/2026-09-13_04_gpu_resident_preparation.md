# GPU 상주10프레임 비교 준비

## 현재 상태

- 최신:채택 평가 재사용·무압축 후보 실행 완료. 후속 GPU 상주 검산 구현·전체 구간 대조는 [새 인계](2026-09-13_05_resident_gpu_audit.md)를 따른다. 아래 실행 대기는 당시 준비 기록이다.

- 최신:current 우선 실행 완료. 채택 평가 재사용·무압축 후보 준비 및 짧은 검산 통과, 사용자 실행 대기.

- 현행:매 프레임 current 시작 후보 준비·짧은 검산 통과. 새 후보10프레임 사용자 실행 대기.

- 최신:병렬 합산 비교 완료. 보조 행렬 중복 제거·기준 재사용 준비, 새 후보만 사용자 실행 대기. 아래 마지막 주제 절 참조.

- 최신:내부 계측 완료·병렬 합산 짧은 검산 통과. 아래 병렬 합산 비교 준비 절이 현행 다음 작업이다.

- 현행:Nsight 대신 GPU 내부 timestamp 계측 준비·실제2단계 검산 통과. 아래 내부 계측 절 참조.

- 최신:본10프레임 검산 통과·생성2.047배 개선. 동일 구간 추적 wrapper 준비·실행 대기. 아래 후속 추적 절 참조.

- 확인일:2026-09-13. GPU 적분·current 보조 행렬 갱신을 연결한 별도 비교 스크립트 준비 완료.
- 기존 v4 직사각형의 느린 구간을 포함한10프레임 입력·동결 코드·의존성을 준비했다.
- 기본2초 저장·메시별 간격 변경 지원. CPU 초기 준비 이후 적분 상태·수렴 제어·행렬 갱신은 GPU에 둔다.
- 작은 통합 실행·실제 메시 짧은 검산만 수행했다. **본10프레임 성능 테스트는 사용자 실행 대기이며 개선율 미확정**.
- 기존 실행은 사용자 본인이 중지한다. 새 wrapper는 남은 teacher 실행을 확인하고 자동 중지하지 않는다.
- 다음:두 backend의 독립 검산·궤적 일치와 비용 분리 결과를 검토한다.
- [실행·근거·재현·한계](../R1_teacher_velocity_reset/timestep_search/gpu_resident/README.md).
- 학습 발행 보류·기존 Gate 유지. 관련 보고·설정·ignored 산출물만 변경. 커밋·푸시 없음.

## 후속 추적 준비 — 2026-09-13

- 본10프레임 비교 완료·정확성 통과, 생성 약2.047배 개선 확인. 초기 준비 대기 상태를 대체한다.
- 개선 전 동결 GPU worker의 동일 구간 Nsight Systems node 추적 wrapper를 준비했다.
- 기존 결과 독립 복사·해시 검증·shim 컴파일·shell 구문 확인 완료. GPU 추적은 아직 실행하지 않았다.
- 다음:사용자 추적 실행 → 내부 비용 분석 → 우선 병목 개선 → 추적 없는 정확성·성능 비교.
- 명령·결과 위치·한계는 [추적 절](../R1_teacher_velocity_reset/timestep_search/gpu_resident/README.md#같은-구간의-gpu-실행-추적--2026-09-13)을 따른다.

## 내부 계측으로 전환

- Nsight node 추적은 WSL+Pascal 지원 범위 문제를 확인했으며 완료 결과 없이 종료139. 실패 자료는 보존한다.
- 동일 동결 GPU 코드에 timer 모듈만 추가하는 wrapper 준비. 계산 중 host 조회 없이50개 경로 누적.
- 실제 메시2단계·조건부 행렬 갱신·기존 독립 검산 통과. 전체10프레임 계측은 사용자 실행 대기.
- 현행 명령·해시·검산은 [내부 계측 절](../R1_teacher_velocity_reset/timestep_search/gpu_resident/README.md#gpu-내부-timestamp-계측--현행-실행-방법).

## 병렬 합산 비교 준비

- 내부 계측 완료·검산 통과. 순차 합산과 보조 행렬 적용이 주요 비용이었다.
- 병렬 합산만 먼저 적용한 실제 메시2단계 검산 통과. 계측 없는 기존/병렬 GPU10프레임 순차 비교 준비.
- [현행 실행·검산](../R1_teacher_velocity_reset/timestep_search/gpu_resident/README.md#병렬-합산-비교--현행-다음-실행).
- 전체 성능은 사용자 실행 대기. 이후 보조 행렬 적용 개선 예정, 물리 설정·기존 결과는 보존한다.

## 보조 행렬 중복 제거·기준 캐시 재사용

- 병렬 합산10프레임 완료·검산 통과, 생성1.502배 개선 확인.
- 사용자 지시로 기존 하이브리드·GPU·병렬 합산의 실행과 검산은 재사용하며 후보만 실행하도록 변경.
- 첫 GMRES cycle 중복 보조 풀이 제거·restart/RHS/zero 검사·실제 메시2단계 검산 통과.
- 후보만 검산하는 경로는 저장된640단계로 기존 검산과 일치 확인. 새 후보10프레임은 실행 대기.
- [현행 실행·근거](../R1_teacher_velocity_reset/timestep_search/gpu_resident/README.md#보조-행렬-적용-개선기준-재사용--현행-실행).

## current 우선 후보 준비

- 중복 제거100.625초·검산 통과. 초기 형상 기준 구간에 선형 반복87.4%가 집중된 점을 확인했다.
- 매 프레임 current로 시작하고64회 갱신 간격을 유지하는 후보 준비. 병렬 합산·중복 제거 포함.
- 실제 첫2단계 반복57→7회·물리 검산 통과. 전체10프레임 성능은 사용자 실행 대기.
- [현행 명령·근거](../R1_teacher_velocity_reset/timestep_search/gpu_resident/README.md#current-우선-후보--현행-다음-실행).

## 채택 평가 재사용·무압축 후보 준비

- current 우선67.887초·검산 통과 이후, 채택 직후 힘 재평가 제거와 무압축 저장 후보 준비.
- 실제2단계·line search 거부 후 채택·기록 정밀도/경계 검증 통과. 물리·검산 기준은 유지한다.
- 이전 기준5개를 재사용하고 후보10프레임만 실행한다. 전체 성능은 사용자 실행 대기.
- [현행 실행·근거](../R1_teacher_velocity_reset/timestep_search/gpu_resident/README.md#채택-평가-재사용무압축-후보--현행-다음-실행).
