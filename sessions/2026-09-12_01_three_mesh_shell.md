# 세 메시 P3 연결과 장기 실행 준비

## 현재 상태

**새 삼각 깃발10초 준비 완료(2026-09-12):** 단기 검증 입력·동결 코드를 별도 묶음에 복사하고 총프레임만600으로 변경했다. 삼각 깃발만 초기rest·0초부터, 시간 한도 없음. hash·설정 동등성·준비 반복 호출·shell 문법·ready0프레임 확인. 계산 미시작. [전용 명령·근거](../R1_teacher_velocity_reset/timestep_search/evidence/triangle_failure_20260912/README.md#새-삼각-깃발-10초-실행-준비). 기존 v4 결과와 wrapper 보존.

**삼각 깃발 입력 수정·단기 검증 완료(2026-09-12):** 옛 입력의 음의 rest 굽힘 강성을 확인한 뒤 고른576면 입력으로 별도0.1초 검증을 완료했다. 전체384단계 GPU 검산·대표3단계 CPU 원식 검산·18파일 hash 통과. [원인·변경·재현·한계](../R1_teacher_velocity_reset/timestep_search/evidence/triangle_failure_20260912/README.md#고른-삼각-격자-01초-검증). 기존 직사각형/손수건10초 완료·옛 삼각 깃발 실패는 보존했다. 물리 코드·물성·바람·dt·허용오차는 유지했다. 다음은 새 메시의 해상도/시간 비교와10초 검증이며 미실행이다. 아래 사용자 재개 대기·본 계산 미시작은 이전 상태다.

**2026-09-12 터미널 종료 후 재개 복구:** 사용자 요청으로 남아 있던 손수건 worker를 종료한 뒤, 재실행이 stale `active_launch.json` 때문에 `worker_error`로 건너뛰는 상태를 복구했다. 손수건 확정312프레임(5.2초)의 trace/metadata/journal 총936파일 SHA256과 동결 원본을 검증하고 `paused`로 변경했다. 기존 report·실행 표시·비용 기록은 [복구 근거](../artifacts/runs/teacher_timestep_search/20260912_three_scenes_10s_v4/handkerchief/recovery/manual_resume_1789212882049194394/recovery_record.json)에 보존했다. Controller 소실 구간의 정확한 실행시간은 미확정이며 비용을 추정 합산하지 않았다.
직사각형600프레임 완료·삼각 깃발3프레임 후 `line_search` 실패는 저장 report 기준이며 변경하지 않았다. 물리 재검산·수렴 인증은 이번 작업에서 수행하지 않았다. 손수건은 기존 wrapper로5.2초부터 사용자 재개 가능하며 이번 복구에서는 계산을 시작하지 않았다. 아래 본 계산 미시작 표기는 이전 준비 시점 기록이다.


**최신 v4 준비:** 사용자 승인으로 세 씬의 이후 모든 프레임에64회 재구축을 적용했다. 첫 씬364frame/6.0667초 보존 재개, 추가 두 씬0초부터·시간 제한 없음. CPU7개 및 세 씬 각64step GPU검산 통과. 본 계산 미시작. [실행·설정 경계·한계](../R1_teacher_velocity_reset/timestep_search/evidence/reuse64_batch_20260912/README.md). 아래 선택 대기는 해소됐다.

**최신 5프레임 비교:** frame159~163에서 rest유지/재구축16/64 세 후보 각320단계 검산 통과.64회 후보가 가장 빨랐고, 현재는 사용자 채택 결정 대기다. 본 실행 설정 미변경. [조건·수치·한계](../R1_teacher_velocity_reset/timestep_search/evidence/preconditioner5_20260912/README.md).

**최신 복구(2026-09-12):** v2 첫 씬은6.0667초에서4시간 한도 종료, 추가 두 씬은 Python 빌드 문자열 검사로 시작 전 종료했다.
사용자 요청으로 빌드 날짜를 호환성 키에서 제외하고 버전·구현·ABI·아키텍처·수치 라이브러리는 검증한다.
v3는 세 씬 모두 시간 한도 없음. 기존364frame의 hash 검증·독립 복사 후 첫 씬 재개, 나머지는0초부터 준비했다.
CPU6개 검사·새 환경의 세 씬 각64step 재계산 통과, 기존 경로 차이0. 본 궤적은 아직 재개하지 않았다.
비용 급증은 current 보조 행렬의 과도한 재구축이 지배했다. 전환 기준/재사용 간격 개선은 자료 조사만 했으며 미적용이다.
[원본·복구·병목 수치·전문 자료](../R1_teacher_velocity_reset/timestep_search/evidence/three_scene_recovery_20260912/README.md). 아래 v2 준비 상태는 이전 기록이다.

확인 기준: 2026-09-12. 세 씬 10초 순차 실행 묶음 준비 완료, 본 계산은 시작하지 않았다.
기존 사각형·저장 삼각 깃발·손수건, 내부30% 목표/EW 상한1e-4·4배 dt를 사용한다.
사용자 수정 반영 v2: 첫 사각형만4시간 제한, 삼각 깃발·손수건은 시간 제한 없이600frame을 목표로 진행한다. 2.5초 수동 정지는 없다.
수치 실패/첫 씬 한도 종료는 보존하고 다음 씬으로 진행한다. 전체 wall time 상한은 없다.
실험: 별도 동결 3프레임 순차 검증·각 씬2+1frame 별도 프로세스 재개·단계 로그/미완료 파일 보존을 검증했다.
세 씬 GPU 원식 검산 통과, 재개 trace 차이0, 이전 CPU 검산 trace와 차이0, CPU5개 검사 통과(추가 두 씬의4시간 초과 진행 검사 포함).
[설정·명령·근거·미완료 범위](../R1_teacher_velocity_reset/timestep_search/evidence/three_scene_batch_20260912/README.md). 기존 초기 연결의13개 검사는 선행 기록이다.
다음은 사용자 실행 후 메시별 총시간·잔차 초과 지점·느린 구간을 검토하는 것이다.
장기는 GPU 힘 재평가 기반 검산이며 긴 구간 CPU 독립 검산·시간 세분/공간 수렴·학습 적격성은 미완료다.
Code/experiments 변경은 미커밋·미푸시. 기존 본 실행과 모든 시험 원본을 보존했다.
