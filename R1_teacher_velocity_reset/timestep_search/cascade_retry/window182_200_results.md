# wind182–200 두 정책 비교 결과

확인2026-09-19. **438회 half2 복구는 모두 성공했으나, 전체 계산+검산 시간 감소는3.21%이며 정책 간 속도 차이가 커 기본 채택을 권하지 않는다.** 기존 기본값/생산/학습 적격성은 유지한다.

## 비교 조건과 완료 범위

GTX1080Ti,driver API13000,기본 M1·force block256/256/256. 복구는 R64다.
복원된182–200번째 프레임 각각의 동일 시작 상태와 원본 forcing에서 기존 Newmark→Gauss8(A)과 Newmark→half2→Gauss8(B)을 각1회 계산했다.
19개의 독립 프레임 비교이며 B를19프레임 연속 적분한 시험이 아니다. 각 후보는 새 초기화 조건이다.
원래 복원 실행의 연속 시간과 섞지 않고 이번38회 실행끼리 비교한다. 실행 순서는 각 쌍 A→B이며 반복/순서 균형 측정은 하지 않았다.

38/38회 정상 완료, 5,922개 채택 단계의 저장 독립 검산 flag0, 상태/검산 유한성·dt합·동결 manifest를 확인했다.
원본 held와 재구성 held의 최대 차이는 전부0이다. 이번 분석에서 solver를 다시 실행하거나 새로운 독립 물리 검산을 수행한 것은 아니다.

| 항목 | 기존 Gauss 복구 | half2 우선 |
| --- | ---: | ---: |
| 19프레임 계산+검산 합계 | 5,176.970초 | 5,011.035초 |
| worker process wall 합계 | 5,349.706초 | 5,226.339초 |
| 기본 Newmark batch | 4,826.322초 | 4,909.699초 |
| 복구 batch | Gauss350.617초 | half101.279초 |
| half 시도/성공 | 0/0 | 438/438 |
| Gauss 복구 횟수 | 436 | 0 |
| 총 GMRES 반복 | 1,747,175 | 1,769,350 |
| 행렬 rebuild | 887 | 888 |
| M1 내부 R64 fallback | 443 | 450 |

계산+검산1.033배, 시간3.205% 감소(약165.9초 절약). worker wall은2.306% 감소다.
실패한 기본 풀이와 M1 내부 fallback 비용을 포함한다. 프로파일러 실행은 아니다.
14/19프레임은 빨랐으나5개는 느려졌다. 최대 단축은185번째25.22%, 최대 증가는182번째24.98%다.
각각1회 측정이므로 작은 평균 차이를 안정적인 장기 가속으로 확정하지 않는다.

기존 시간의 약93.2%는 이미 기본 Newmark batch가 사용했다. 복구 비용을약249.3초 줄였지만,
복구 뒤 달라진 상태를 포함한 기본 Newmark 비용이약83.4초 늘어 이득 일부가 상쇄됐다.
GMRES 반복도약1.27% 증가했다. 실패 후 전환하는 방식은 실패 판정까지의 반복 비용을 이미 지불한다.
반복 증가의 세부 수치 원인을 분해 측정한 것은 아니므로 특정 커널/정밀도만 원인으로 확정하지 않는다.

## 수치 차이와 판정

| 프레임별 정책 간 차이 | 최소 | 중앙값 | 최대 |
| --- | ---: | ---: | ---: |
| 위치 성분 최대 차이 | 0.163mm | 0.221mm | 0.349mm |
| 속도 질량 가중 상대 L2 | 22.08% | 28.69% | 49.00% |
| 속도 성분 최대 차이 | 0.783m/s | 1.110m/s | 1.751m/s |

질량 가중 속도 차이는 sqrt(δvᵀMδv / v_AᵀMv_A)다. 원시 hi/lo를 longdouble로 합성하여 끝 상태를 비교했다.
운동에너지 차이의 절대 최댓값은약9.60e-5 J, 기존 운동에너지 대비 차이의 절대 최댓값은약0.404%다.
작은 위치/에너지 차이나 기존 운동방정식 검산 통과가 속도장 일치를 뜻하지 않는다.
위 값은 **기존 정책 결과와의 차이**이며 참해 오차가 아니다. 새 세분화 참조는 계산하지 않았다.

- 수렴/기존 독립 검산: 전체 통과.
- half2의 고부하 복구: 관측한438회 전부 성공. half 실패→Gauss 경로는 이번 실제 표본에서는 미발생.
- 성능: 총시간 소폭 감소 관측, 프레임별 이득/손해 혼재.
- 동일 정확도/학습 적격성: budget_not_defined. 큰 속도 차이를 승인된 오차로 해석하지 않는다.
- 운영 채택: 기존 Newmark→Gauss8 유지. 자동 기본값 변경 없음.

## 원본

Canonical: `experiments/artifacts/runs/teacher_timestep_search/gpu_gtx1080ti_wind120_to200_reference_v1/comparison/`.
그 안의 `window_status.json`, `execution.json`, `frames.csv`, `attempts.csv`, `pairs.csv`, `runs/*/result.json` 및 `result.npz`가 원시 근거다.
`manifest.json`의 동결 입력/runtime hash를 확인했다. GTX1080Ti 로컬 저장 결과만 읽었고 원격 실행/fetch/download는 하지 않았다.
5070 성능이나 미측정 연속 궤적 결과로 확장하지 않는다. [재현 스크립트](resume120.md#2026-09-19-복원-완료-후-한-번에-비교-실행).
